# shared-protocol
The single source of truth for our data models. This holds your Protobufs, JSON schemas, or OpenAPI specs that both the sdk and core repos will import to ensure the "Attestation Signature" shape never drifts.
